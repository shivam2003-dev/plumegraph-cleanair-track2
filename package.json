{
  "name": "plumegraph-cleanair-track2",
  "version": "0.2.0",
  "private": true,
  "description": "VayuLens hyperlocal pollution response prototype",
  "scripts": {
    "generate:ai": "node scripts/generate-ai-insights.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check plumegraph-core.js && node --check plumegraph-pages.js && node --check scripts/generate-ai-insights.mjs"
  }
}
