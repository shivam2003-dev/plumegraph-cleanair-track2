{
  "name": "plumegraph-cleanair-track2",
  "version": "0.2.0",
  "private": true,
  "description": "PlumeGraph hyperlocal pollution response prototype",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check plumegraph-core.js && node --check plumegraph-pages.js"
  }
}
